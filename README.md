# PythonApplication1

## Streamlit Community Cloud deployment
1. Push this repository to GitHub.
2. In Streamlit Community Cloud, create a new app from this repo.
3. Set the app entrypoint to `streamlit_app.py`.
4. Use the root `requirements.txt` for dependency installation.
5. Keep secrets in `.streamlit/secrets.toml` and do not commit sensitive values.
